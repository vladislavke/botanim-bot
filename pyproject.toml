[tool.poetry]
name = "botanim-bot"
version = "0.1.0"
description = ""
authors = ["vlad <vladkersantinov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
python-telegram-bot = "^20.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
